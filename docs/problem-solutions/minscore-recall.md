# 通过 minScore 解决召回准确率低的问题

## 问题

只设置 Top-K 会把固定数量的片段送给大模型。即使后面的片段相关性很弱，也会因为凑满数量进入上下文。

这会让模型看到无关信息，增加误答概率。

## 解决思路

在 Rerank 后增加最低分阈值：

- 高于阈值的片段才进入最终上下文。
- 低于阈值的片段直接过滤。
- 当高质量片段不足时，允许上下文变少，而不是强行凑满。

## 产品价值

RAG 不是给模型塞越多越好，而是给得准更重要。minScore 能减少弱相关片段，提升答案稳定性。
